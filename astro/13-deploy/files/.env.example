# Variables de entorno — copiar a .env y completar

# Autenticación (Módulo 11)
ADMIN_USER=admin
ADMIN_PASSWORD=cambiar-esta-contrasena
SESSION_SECRET=cadena-aleatoria-muy-larga-de-al-menos-32-caracteres

# API pública (accesible en cliente — prefijo PUBLIC_)
# PUBLIC_API_URL=https://api.ejemplo.com
