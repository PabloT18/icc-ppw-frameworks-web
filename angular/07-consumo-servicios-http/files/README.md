# Files — Módulo 07

Archivos de referencia para el módulo de consumo de servicios HTTP.

## Índice

| Archivo | Práctica | Descripción |
|---|---|---|
| `simpsons.interface.ts` | A | Interfaces: `SimpsonsCharacter`, `SimpsonsResponse`, `Options` |
| `simpsons.service.ts` | A, B, C | Servicio con `getCharacters`, `getCharactersOptions`, `getCharacterById` |
| `simpsons-page.ts` | B, C | Componente de listado con `rxResource` y `PaginationService` |
| `simpsons-page.html` | B, C | Template completo con cards DaisyUI y controles de paginación |
| `pagination.service.ts` | B | Servicio de paginación reactivo con `toSignal` y `ActivatedRoute` |
| `simpson-detail-page.ts` | C | Componente de detalle con `rxResource` |
| `simpson-detail-page.html` | C | Template de detalle con foto, frases y badge de estado |

## Uso

Copiar el contenido de cada archivo a la ruta indicada en la práctica correspondiente.
No copiar sin leer las instrucciones de cada paso — algunos archivos requieren ajustar rutas de importación.
